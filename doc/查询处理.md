> 将用户提交的查询转换为高效的执行计划

## 查询优化

### 工作流程

1. 查询编译：将用户提交的SQL查询解析为内部表示形式（如解析树或抽象语法树）。
2. 查询优化：对解析后的查询进行优化，生成一个高效的执行计划；优化器会考虑多种执行策略，并选择代价最低的方案。
3. 查询执行：根据优化器生成的执行计划，实际执行查询操作，访问数据库中的数据，并返回结果。



1. 初始逻辑查询计划生成：将语法分析树转换为关系代数表达式树——逻辑查询计划（使用关系代数）
2. 查询重写：将初始逻辑查询计划转换为优化的逻辑查询计划
   1. 基于代数转换规则
3. 估计中间结果大小结果大小